[package]
name = "pyobox-worktree"
version = "0.1.0"
edition = "2021"
description = "Git worktree management for agent isolation"

[dependencies]
pyobox-types = { path = "../pyobox-types" }
serde = { version = "1", features = ["derive"] }
anyhow = "1"
