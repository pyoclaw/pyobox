[package]
name = "pyobox-bootstrap"
version = "0.1.0"
edition = "2021"
description = "System provisioning engine — OS detection, package install, dotfiles"

[dependencies]
pyobox-types = { path = "../pyobox-types" }
serde = { version = "1", features = ["derive"] }
anyhow = "1"
