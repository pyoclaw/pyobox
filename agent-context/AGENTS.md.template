# Agent Instructions (auto-generated by pyobox)

You are inside a pyobox-managed worktree.

- **Main repo**: {{PYOBOX_REPO}}
- **Your branch**: {{PYOBOX_BRANCH}}
- **Worktree ID**: {{PYOBOX_WORKTREE_ID}}
- **Shared DB**: {{PYOBOX_DB_URL}} (minisqlite)
- **Other agents**: Run `pyobox list-agents` to see who else is working

## Rules
1. Do NOT create your own git worktrees. Use the worktree at `{{PYOBOX_WORKTREE_PATH}}`.
2. All code changes go in this worktree. Push from the main repo.
3. The shared database is at `{{PYOBOX_DB_URL}}`. Schema in `services/minisqlite/migrations/`.
4. When done: run `pyobox destroy-agent {{AGENT_NAME}}` to clean up.
